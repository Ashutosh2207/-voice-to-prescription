# Core dependencies
streamlit>=1.35.0
fastapi==0.110.1
uvicorn==0.29.0
pydantic>=2.10.0
pydantic-settings==2.3.3

# Speech-to-Text (Local/Free) - faster-whisper has wheels
faster-whisper
torch==2.13.0
torchaudio==2.11.0

# NLP/Extraction (Pure Python - no compilation)
python-dotenv==1.0.1
httpx==0.27.0

# Audio processing (use pydub with ffmpeg, no librosa/soundfile)
pydub==0.25.1

# Utilities - use numpy 2.x with pre-built wheels if available
numpy
pandas
python-multipart==0.0.9

# Testing
pytest==8.2.1
pytest-asyncio==0.23.3