[project]
name = "voice-to-prescription"
version = "1.0.0"
description = "AI-powered Voice to Prescription system"
requires-python = ">=3.10"
dependencies = [
    "fastapi==0.110.1",
    "uvicorn==0.29.0",
    "python-multipart"
]

[tool.vercel]
entrypoint = "app.api:app"